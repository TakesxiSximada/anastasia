machine:
  python:
    version: 3.5.0

dependencies:
  pre:
    - git submodule init
    - git submodule update

  override:
    - pip install -r requirements/docs.txt

test:
  override:
    - make docs output=$CIRCLE_ARTIFACTS
